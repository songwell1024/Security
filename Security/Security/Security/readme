package System下：
    MyFrame是界面框架
    Security是整个系统的主文件
package EncryptionAlgorithm下：
    BytesAndHex是进制准换的辅助函数
            其余是加密算法和Hash函数
package Font下：
	界面的字体设置文件
	
目前的菜单栏的功能只支持明文去的文本的编辑，而且界面也不能自适应大小这是目前存在的还需要日后完善的功能吧